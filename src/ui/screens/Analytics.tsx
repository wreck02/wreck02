import type { Nav } from '../App';


export function Analytics(_props: { nav: Nav }) {
  return (
    <main className="page">
      <div className="card">
        <h1>Analytics</h1>
        <p className="muted">This screen is not built yet.</p>
      </div>
    </main>
  );
}

export default Analytics;
